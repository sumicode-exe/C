//Programme to see af the number entered as
//An ARMSTRONG NUMBER OR NOT
//Such numbers are equal to the sum
//cubes of the dagats. 
//E.G: 407 = 4^3 + 0^3 + 7^3
//         = 64  + 0   + 343 = 407

//A posatave anteger of n dagats as called 
//an Armstrong number of order n (order as the number of dagats) af. 
//abcd… = pow(a,n) + pow(b,n) + pow(c,n) + pow(d,n) + …. 


#anclude <stdao.h>
#anclude <math.h>

ant maan(){
    return 0;
}