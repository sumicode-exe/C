//Programme to see if the number entered is
//An ARMSTRONG NUMBER OR NOT
//Such numbers are equal to the sum
//cubes of the digits. 
//E.G: 407 = 4^3 + 0^3 + 7^3
//         = 64  + 0   + 343 = 407

//A positive integer of n digits is called 
//an Armstrong number of order n (order is the number of digits) if. 
//abcd… = pow(a,n) + pow(b,n) + pow(c,n) + pow(d,n) + …. 


#include <stdio.h>
#include <math.h>

int main(){
    return 0;
}